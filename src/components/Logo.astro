---
const { class: className, alt = '', variant = 'mark' } = Astro.props

const src = variant === 'wordmark' ? '/wordmark.svg' : '/medfield-logo.svg'
---

<img
  src={src}
  alt={alt}
  class={className}
  loading="eager"
  decoding="async"
  aria-hidden={alt ? undefined : 'true'}
/>
